<%= render partial: 'teacher/partials/dashboard_side_bar' %>

<%= form_for @video, url:teacher_course_videos_path(params[:course_id], @video), html: { method: :post } do |form| %>
  <%= form.attachment_field :file, direct: true %>
  <%= form.submit %>
<% end %>
